# 密钥与运行数据，切勿提交
.env
# 忽略 data 目录内容，但保留示例文件（用 /data/* 而非 /data/，否则目录被整体排除会使 negation 失效）
/data/*
!/data/config.yaml.example

# 本地工具缓存
/.playwright-cli/

# nginx-proxy / acme 若改用 bind mount 时的运行时目录
/nginx/
